[project]
name = "midi-drum-remapper"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "mido>=1.3.0",
    "pyside6>=6.6.0",
    "python-rtmidi>=1.5.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
